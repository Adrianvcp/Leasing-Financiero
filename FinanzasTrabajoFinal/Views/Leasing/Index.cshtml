@model IEnumerable<FinanzasTrabajoFinal.Models.Banco>

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta tags -->
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

    <link href="~/css/style.css" rel='stylesheet' type='text/css' media="all">

</head>
<body class="fondo-negro">

    <h1 class="header-w3ls">
        Leasing
    </h1>z

    @using (Html.BeginForm("Simulador", "Leasing"))
    {
        <div class="container">
            <div class="main-bothside">
                <form action="#" method="post">
                        
                    <link href="~/css/jquery-ui.css" rel="stylesheet" />
                    
                    <div>
                        <div class="form-mid-w3ls">
                            <p>Entidad Financiera</p>
                            @Html.DropDownList("NombreBando", new SelectList(ViewBag.data, "idBanco", "NombreBanco"))
                        </div>
                    </div>

                    <div>
                        <div class="form-mid-w3ls">
                            <p>Carro</p>
                            @Html.DropDownList("carro", new SelectList(ViewBag.carros, "idCarro", "NCarro"), new
                       {
                           onchange = @"as"
                       })
                        </div>
                        <p id="demo">sdfsdf</p>
                    </div>


                    @*<div>
                        <div class="form-mid-w3ls">
                            <p>% Cuota Inicial</p>
                            @Html.TextBox("ci","",new {@placeholder="Cuota Incial"})
                        </div>
                    </div>*@ 


                    <div>
                        <div class="form-mid-w3ls">
                            <p>Frecuencia</p>
                            @Html.DropDownList("fr", new SelectList(ViewBag.Frecuencia, "idFrecuencia", "NNombre"))
                        </div>
                    </div>

                    <div>
                        <div class="form-mid-w3ls">
                            <p>Plazo de Gracia</p>
                            @Html.DropDownList("pl", new SelectList(ViewBag.plazoGracia, "idPlazoGracia", "NNombre"))
                            @Html.TextBox("CantCu", "", new { @placeholder = "Cantidad Cuotas"})
                        </div>
                    </div>



                    <div>
                        <div class="form-mid-w3ls">
                            <p>Numero de Años</p>
                            @Html.TextBox("anios","",new { @placeholder="Numero de Años"})
                        </div>
                    </div>

                    <input type="submit" value="Simular Leasing" />
                </form>
            </div>
        </div>
    }


    <script>
        $("#number").on({
            "focus": function (event) {
                $(event.target).select();
            },
            "keyup": function (event) {
                $(event.target).val(function (index, value) {
                    return value.replace(/\D/g, "")
                                .replace(/([0-9])([0-9]{2})$/, '$1.$2')
                                .replace(/\B(?=(\d{3})+(?!\d)\.?)/g, ",");
                });
            }
        });
    </script>

    <script>
        $("#number").on({
            "focus": function (event) {
                $(event.target).select();
            },
            "keyup": function (event) {
                $(event.target).val(function (index, value) {
                    return value.replace(/\D/g, "")
                                .replace(/([0-9])([0-9]{2})$/, '$1.$2')
                                .replace(/\B(?=(\d{3})+(?!\d)\.?)/g, ",");
                });
            }
        });
    </script>

    <script>
        function as() {
            document.getElementById("demo").innerHTML = "Hello World";
            }
    </script>




</body>
</html>